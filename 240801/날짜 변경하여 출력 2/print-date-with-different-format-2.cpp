#include <iostream>
using namespace std;

int main() {
    int dd, mm, yyyy;
    char a;
    cin >> mm >> a >> dd >> a >> yyyy;
    cout << yyyy << "." << mm << "." << dd;// 여기에 코드를 작성해주세요.
    return 0;
}